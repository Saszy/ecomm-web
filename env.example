# AWS S3 Configuration
# Copy this file to .env.local and fill in your actual values

# AWS Region
NEXT_PUBLIC_AWS_REGION=us-east-1

# S3 Bucket Name for storing user events
NEXT_PUBLIC_S3_BUCKET_NAME=your-events-bucket

# AWS Access Key ID (for S3 access)
NEXT_PUBLIC_AWS_ACCESS_KEY_ID=your_access_key_id

# AWS Secret Access Key (for S3 access)
NEXT_PUBLIC_AWS_SECRET_ACCESS_KEY=your_secret_access_key

# Optional: AWS Session Token (if using temporary credentials)
# NEXT_PUBLIC_AWS_SESSION_TOKEN=your_session_token

# Optional: S3 Bucket Prefix for organizing events
# NEXT_PUBLIC_S3_EVENTS_PREFIX=events/

# Optional: Data retention period in days
# NEXT_PUBLIC_EVENTS_RETENTION_DAYS=90

# Optional: Batch upload size for events
# NEXT_PUBLIC_EVENTS_BATCH_SIZE=100
